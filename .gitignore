.env
.env.local
.env*.local
.next/
node_modules/
out/
.vercel
*.tsbuildinfo
next-env.d.ts

# DB-Backups (lokal, nicht committen)
backups/

# Lokaler Stripe-Test-Output
scripts/.stripe-prices.env
scripts/.bc-*

# Playwright-Artefakte (Baselines in e2e/**-snapshots werden committet)
test-results/
playwright-report/


# CRM-Leads: PII (Namen, Handynummern, E-Mails) — niemals ins öffentliche Repo
outreach/*.csv
